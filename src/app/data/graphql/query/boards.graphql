query Boards($boardsPageInput: PageInput!) {
  boards(pageInput: $boardsPageInput) {
    pageInfo {
      offset
      limit
      hasNextPage
      hasPreviousPage
    }
    nodes {
      id
      name
    }
    count
  }
}
